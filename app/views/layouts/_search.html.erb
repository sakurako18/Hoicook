  <%= form_with url: search_path, local: true, method: :get do |f| %>

  <div class="search_form">
    <%= f.text_field :word %>
    <%= f.select :range, options_for_select([['Recipe'], ['RecipeIngredients']]) %>
    <%= f.submit "検索", class: "btn btn-outline-secondary btn-sm" %>
  </div>
    <% end %>