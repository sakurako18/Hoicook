  <h5>月齢</h5>

    <%= form_with model: @recipe, url: user_recipes_path, method: :get do |f| %>
      <% Tag.all.each do |t| %>
        <%= check_box :tag_ids, t.name %> <%= t.name %><br>
      <% end %>


  <h5>ジャンル検索</h5>

      <%= f.collection_check_boxes(:genre_ids, @genres, :id, :name) do |b| %>
        <%= b.label { b.check_box + b.text } %>
      <% end %>

      <%#= f.check_box 'genres[][genre_id]', value: genre.id %> <%#= genre.name %><br>
        <%#= check_box :genre_ids, genre.id %> <%#= genre.name %><br>


    <%= f.submit '検索' %>
    <% end %>